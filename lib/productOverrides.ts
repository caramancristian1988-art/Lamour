// Presentation-only overrides for specific product slugs — empty by default.
// Add entries here if you need to override a product's display image/badge/name
// without editing the underlying database record.
export const localProductImages: Record<string, string> = {};
export const localProductBadges: Record<string, string> = {};
export const localProductNames: Record<string, string> = {};
